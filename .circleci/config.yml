version: 2.1

orbs:
  heroku: circleci/heroku@0.0.10

workflows:
  test-build-deploy:
    jobs:
      - build
      - heroku/deploy-via-git:
          requires:
            - build
          filters:
            branches:
              only: prod

jobs:
  test:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: npm ci && npm run test

  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: npm ci && npm build

